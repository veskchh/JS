function SpeedLimitChecker(speed, area) {
    if (area === "motorway") {
        if (speed > )
    }
    else if (area === "interstate") {

    }
    else if (area === "city") {

    }
    else if (area === "residential") {

    }
}